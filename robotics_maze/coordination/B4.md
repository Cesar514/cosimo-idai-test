# B4 Progress Log

## 2026-02-26
- Implemented baseline grid planners in `src/planners.py`:
  - A* (`astar`)
  - Dijkstra (`dijkstra`)
  - Greedy Best-First (`greedy_best_first`, aliases: `greedy`, `gbfs`)
- Added a planner registry interface:
  - `register_planner(name, planner, overwrite=False)`
  - `get_planner(name)`, `list_planners()`
  - `plan_path(planner_name, grid, start, goal, **kwargs)`
- Standardized planner return payload with required metrics:
  - `path` (list of `(row, col)` points)
  - `expanded_nodes`
  - `runtime_sec`, `runtime_ms`
- Added reusable heuristic registry/utilities in `src/heuristics.py`:
  - `register_heuristic`, `get_heuristic`, `list_heuristics`, `resolve_heuristic`
  - Built-in heuristics: Manhattan, Euclidean, Chebyshev
